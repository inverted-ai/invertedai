version: "3.3"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: "host"
    # restart: always
    environment:
      DISPLAY: $DISPLAY
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
    privileged: true
    # command: ["./CarlaUE4.sh", "-fps=10"]
