version: "3.3"

services:
  server:
    image: "carlasim/carla:0.9.13"
#    build:
#      context: .
#      dockerfile: Dockerfile-additional-maps
    network_mode: "host"
    # restart: always
    environment:
      DISPLAY: $DISPLAY
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
    privileged: true
    # command: ["./CarlaUE4.sh", "-fps=10"]
